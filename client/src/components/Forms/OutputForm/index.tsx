import React from 'react';
import { Container } from 'react-bootstrap';

const OutputForm: React.FunctionComponent = () => (
  <Container>
    <p className="text-light">Hello Output page</p>
  </Container>
);

export default OutputForm;
