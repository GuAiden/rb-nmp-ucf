import React from 'react';
import { Container } from 'react-bootstrap';

const ServerForm: React.FunctionComponent = () => (
  <Container>
    <p className="text-light">Hello server page</p>
  </Container>
);

export default ServerForm;
